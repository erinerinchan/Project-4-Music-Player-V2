function PageTitle({ name }) {
  return (
    <div className="font-bold mt-4 mb-1 text-xs uppercase">
      <h1 className="text-base font-bold leading-7 tracking-tighter normal-case text-[#fff]">{name}</h1>
    </div>
  )
}

export default PageTitle
